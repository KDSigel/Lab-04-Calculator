export function sum(addX, addY) {
    return addX + addY;
}

export function difference(subX, subY) {
    return subX - subY;
}

export function product(multX, multY) {
    return multX * multY;
}

export function quotent(divX, divY) {
    return divX / divY;
}